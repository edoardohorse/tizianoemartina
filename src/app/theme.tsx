'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	palette: {
		primary: {
			main: '#158e83',
		},
		secondary: {
			main: '#e2f0ed',
		},
	},
});

export default theme;