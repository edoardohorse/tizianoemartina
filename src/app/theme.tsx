'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	palette: {
		primary: {
			main: '#709075',
		},
		secondary: {
			main: '#e2f0ed',
		},
	},
});

export default theme;